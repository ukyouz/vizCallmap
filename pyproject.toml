[project]
name = "vizcallmap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "argini",
    "c-defineparser",
    "tree-sitter==0.25.2",
    "tree-sitter-c==0.24.1",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
include = ["src"]

[tool.uv.sources]
argini = { git = "https://github.com/ukyouz/py-argini", rev = "main" }
c-defineparser = { git = "https://github.com/ukyouz/C-define-Parser", rev = "main" }
